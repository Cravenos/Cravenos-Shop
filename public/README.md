# Cravenos Services

Static website for Cravenos Services.

## GitHub Pages
Upload the contents of this folder to a GitHub repository, then enable **Settings → Pages → Deploy from a branch** and select the `main` branch and `/ (root)` folder.

The site is static and uses `index.html` as its homepage.
